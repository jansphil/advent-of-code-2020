const test = require('ava').default
const { part1, part2 } = require('./<<NAME>>')

const input = []

test('part 1', (t) => {
  t.is(part1(input), 42)
})

test('part 2', (t) => {
  t.is(part2(input), 42)
})
